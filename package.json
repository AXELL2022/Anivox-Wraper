{
  "name": "tauri-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "NO_STRIP=1 tauri build",
    "build:arch": "NO_STRIP=1 tauri build --bundles deb && makepkg -e -f",
    "install:arch": "bun run build:arch && sudo pacman -U --noconfirm anivox-*.pkg.tar.zst",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
